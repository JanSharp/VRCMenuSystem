
- [ ] Show an effective lockstep loading screen, during which none of the pages are accessible, because we do not know the local player's permissions and many pages won't even work without lockstep being initialized
- [ ] When missing permissions to see any page at all, show some info text in the UI
